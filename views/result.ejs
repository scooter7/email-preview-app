<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Preview Results</title>
    <!-- Link to the shared stylesheet in the public directory -->
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Additional styles for better presentation on the results page */
        body {
            background-color: #f7fafc;
            color: #1a202c;
        }
        .container {
            /* Increased max-width for a wider layout */
            max-width: 1600px;
            margin: 0 auto;
            padding: 2rem;
        }
        h1 {
            font-size: 2.25rem;
            font-weight: bold;
            color: #2d3748;
            text-align: center;
            margin-bottom: 2rem;
        }
        .screenshots {
            display: grid;
            /* Increased minmax value to make columns wider, resulting in larger previews */
            grid-template-columns: repeat(auto-fill, minmax(480px, 1fr));
            gap: 2.5rem; /* Increased gap for more breathing room */
        }
        .screenshot-item {
            background-color: #ffffff;
            border: 1px solid #e2e8f0;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            padding: 1.5rem;
            text-align: center;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        .screenshot-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .screenshot-item h3 {
            font-size: 1.125rem;
            font-weight: 600;
            color: #4a5568;
            margin-bottom: 1rem;
            text-transform: capitalize;
            word-break: break-all;
        }
        .screenshot-item img {
            max-width: 100%;
            height: auto;
            border: 1px solid #cbd5e0;
            border-radius: 0.25rem;
        }
        .error-message {
            background-color: #fff5f5;
            color: #c53030;
            border: 1px solid #fc8181;
            padding: 1rem;
            border-radius: 0.25rem;
            font-family: monospace;
            font-size: 0.875rem;
            word-break: break-word;
        }
        .back-link {
            display: inline-block;
            margin-top: 3rem;
            padding: 0.75rem 1.5rem;
            background-color: #4299e1;
            color: white;
            font-weight: bold;
            text-decoration: none;
            border-radius: 0.375rem;
            transition: background-color 0.2s;
        }
        .back-link:hover {
            background-color: #3182ce;
        }
        .center-link {
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Email Preview Results</h1>
        <div class="screenshots">
            <% results.forEach(result => { %>
                <div class="screenshot-item">
                    <h3><%= result.name.replace(/_/g, ' ') %></h3>
                    <% if (result.filename) { %>
                        <img src="/screenshots/<%= result.filename %>" alt="Screenshot for <%= result.name %>">
                    <% } else { %>
                        <div class="error-message">
                            <p><strong>Error generating preview:</strong></p>
                            <p><%= result.error || 'An unknown error occurred.' %></p>
                        </div>
                    <% } %>
                </div>
            <% }); %>
        </div>
        <div class="center-link">
             <a href="/" class="back-link">Test Another Email</a>
        </div>
    </div>
</body>
</html>
